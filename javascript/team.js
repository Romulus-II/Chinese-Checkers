var team = [];

function createTeam(index, color){

    // Create pieces array;
    var new_piece = []

    team[index-1] = {
        color: color,
        piece: new_piece,
        active: false,
        in_goal: false
    }
}

function createPiece(s, color){

}